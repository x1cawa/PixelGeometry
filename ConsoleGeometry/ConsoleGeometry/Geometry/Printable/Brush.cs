using System;
using System.Collections.Generic;
using System.Text;
using ConsoleGeometry.ConsoleHelper;

namespace ConsoleGeometry.Geometry.Printable
{
    public class Brush : IBrush
    {
        /*public char Symbol { get; set; }
        public ConsoleState.Color ConsoleColor { get; set; }

        public Brush() : this (' ', ConsoleState.Color.Default()) { }
        public Brush(char symbol, ConsoleState.Color color) => SetBrash(symbol, color );

        public void SetBrash(char symbol, ConsoleState.Color color)
        {
            Symbol = symbol;
            ConsoleColor = color;
        }*/
    }
}
